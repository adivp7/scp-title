This is to assert that this extension does not collect or transfer any user data.

All the data processed by the extension (which only consists of data from scp-wiki) is only processed on the user's machine. 
